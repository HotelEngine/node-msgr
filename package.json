{
  "name": "node-msgr",
  "version": "0.4.0",
  "description": "A node AMQP client built to play nice with the msgr Rails gem.",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "repository": "https://github.com/HotelEngine/node-msgr",
  "author": "Hotel Engine",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf ./dist/*",
    "build": "./node_modules/.bin/tsc",
    "prepublish": "npm run clean && npm run build"
  },
  "devDependencies": {
    "@types/amqplib": "0.5.x",
    "@types/node": "8.x.x",
    "typescript": "2.4.x"
  },
  "dependencies": {
    "amqplib": "0.5.x",
    "uuid": "3.1.x"
  }
}
